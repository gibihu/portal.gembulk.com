import api from "@/routes/api";
import { UserType } from "@/types/user";

export function GetSenderByUser(user: UserType, query: string[]){
    const fetchData = async () => {
        // const way = api.senders.
    };
}